services:
  chroma:
    image: chromadb/chroma:latest
    container_name: next-document-research-chroma
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data/chroma:/data
